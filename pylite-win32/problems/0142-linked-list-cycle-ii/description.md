# 142. 环形链表 II

## 题目描述

给定一组节点及每个节点的后继下标，找出从头节点出发首次进入环的节点。下标 `-1` 表示空指针。

## 输入格式

第一行是 `n head`。第二行是 `n` 个整数，第 `i` 个数表示节点 `i` 的后继下标。

## 输出格式

输出环入口节点的下标；不存在环时输出 `-1`。

## 样例

```text
输入：
4 0
1 2 3 1

输出：
1
```

## 数据范围

- 0 <= n <= 100000
- -1 <= head, next[i] < n
