# 39. 组合总和

## 题目描述

给定一组互不相同的正整数和目标值，找出所有元素和等于目标值的组合。每个候选数字可以重复使用。

## 输入格式

第一行是 `n target`。第二行是 `n` 个互不相同的正整数。

## 输出格式

组合内部按升序输出，每行一个组合；所有组合按字典序排列，没有答案时输出 `NONE`。

## 样例

```text
输入：
4 7
2 3 6 7

输出：
2 2 3
7
```

## 数据范围

- 1 <= n <= 30
- 1 <= target <= 40
