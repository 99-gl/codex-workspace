# 131. 分割回文串

## 题目描述

给定一个字符串，将它分割成若干子串，使每个子串都是回文串，并输出所有分割方案。

## 输入格式

输入一行只含小写字母的非空字符串。

## 输出格式

每行输出一种方案，片段之间使用 `|` 分隔；所有方案按字典序排列。

## 样例

```text
输入：
aab

输出：
a|a|b
aa|b
```

## 数据范围

- 1 <= 字符串长度 <= 16
