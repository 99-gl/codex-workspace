# 141. 环形链表

## 题目描述

给定一组节点及每个节点的后继下标，判断从头节点出发是否会进入环。下标 `-1` 表示空指针。

## 输入格式

第一行是 `n head`，`head` 是头节点下标。第二行是 `n` 个整数，第 `i` 个数表示节点 `i` 的后继下标。

## 输出格式

存在环输出 `true`，否则输出 `false`。

## 样例

```text
输入：
4 0
1 2 3 1

输出：
true
```

## 数据范围

- 0 <= n <= 100000
- -1 <= head, next[i] < n
