# 101. 对称二叉树

## 题目描述

判断一棵二叉树是否关于根节点左右镜像对称。

## 输入格式

第一行是层序标记数量 `n`。第二行给出 `n` 个层序标记，空节点写作 `null`。

## 输出格式

对称输出 `true`，否则输出 `false`。

## 样例

```text
输入：
7
1 2 2 3 4 4 3

输出：
true
```

## 数据范围

- 0 <= 节点数 <= 10000
