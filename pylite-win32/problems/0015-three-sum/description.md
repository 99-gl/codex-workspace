# 15. 三数之和

## 题目描述

给定整数数组，找出所有和为 0 且下标互不相同的三元组。答案不能包含重复三元组。

## 输入格式

第一行是整数 `n`。第二行是 `n` 个整数。

## 输出格式

每行按升序输出一个三元组，所有三元组按字典序排列；没有答案时输出 `NONE`。

## 样例

```text
输入：
6
-1 0 1 2 -1 -4

输出：
-1 -1 2
-1 0 1
```

## 数据范围

- 0 <= n <= 3000
- -10^5 <= ai <= 10^5
