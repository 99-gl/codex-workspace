# 234. 回文链表

## 题目描述

判断一个单链表从前向后和从后向前读取时是否完全相同。

## 输入格式

第一行是节点数 `n`。第二行按链表顺序给出 `n` 个节点值。

## 输出格式

是回文链表输出 `true`，否则输出 `false`。

## 样例

```text
输入：
4
1 2 2 1

输出：
true
```

## 数据范围

- 0 <= n <= 100000
