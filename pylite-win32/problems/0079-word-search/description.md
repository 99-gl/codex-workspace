# 79. 单词搜索

## 题目描述

给定一个字符网格和一个单词，判断能否通过依次访问上下左右相邻的格子拼出该单词。同一个格子不能重复使用。

## 输入格式

第一行是 `m n`。随后 `m` 行是长度为 `n` 的字符行，最后一行是待查找单词。

## 输出格式

能够找到输出 `true`，否则输出 `false`。

## 样例

```text
输入：
3 4
ABCE
SFCS
ADEE
ABCCED

输出：
true
```

## 数据范围

- 1 <= m, n <= 20
- 1 <= 单词长度 <= 100
