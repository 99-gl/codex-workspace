# 139. 单词拆分

## 题目描述

给定字符串和一个单词字典，判断字符串能否由一个或多个字典单词按顺序拼接而成。字典单词可以重复使用。

## 输入格式

第一行是字符串 `s`。第二行是字典大小 `m`。第三行是 `m` 个不含空格的单词。

## 输出格式

可以拆分输出 `true`，否则输出 `false`。

## 样例

```text
输入：
leetcode
2
leet code

输出：
true
```

## 数据范围

- 1 <= 字符串长度 <= 300
- 1 <= m <= 1000
