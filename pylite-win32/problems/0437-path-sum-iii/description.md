# 437. 路径总和 III

## 题目描述

给定一棵二叉树和目标值，统计节点和等于目标值的向下路径数量。路径可以从任意节点开始和结束，但只能从父节点走向子节点。

## 输入格式

前两行使用层序标记格式描述树；最后一行给出目标值 `target`。

## 输出格式

输出满足条件的路径数量。

## 样例

```text
输入：
11
10 5 -3 3 2 null 11 3 -2 null 1
8

输出：
3
```

## 数据范围

- 0 <= 节点数 <= 10000
- 节点值和目标值在 32 位整数范围内
