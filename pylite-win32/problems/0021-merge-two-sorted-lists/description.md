# 21. 合并两个有序链表

## 题目描述

给定两个非递减单链表，将它们合并为一个非递减链表。

## 输入格式

第一行是 `n m`。第二行是第一个链表的 `n` 个值，第三行是第二个链表的 `m` 个值。

## 输出格式

输出合并后的节点值；结果为空时输出 `EMPTY`。

## 样例

```text
输入：
3 3
1 2 4
1 3 4

输出：
1 1 2 3 4 4
```

## 数据范围

- 0 <= n, m <= 5000
