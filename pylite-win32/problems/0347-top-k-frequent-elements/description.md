# 347. 前 K 个高频元素

## 题目描述

给定整数数组，返回出现频率最高的 `k` 个不同整数。频率相同时，数值较小者排在前面。

## 输入格式

第一行是 `n k`。第二行是 `n` 个整数。

## 输出格式

按频率降序输出结果；频率相同按数值升序。

## 样例

```text
输入：
6 2
1 1 1 2 2 3

输出：
1 2
```

## 数据范围

- 1 <= k <= 不同整数个数
- 1 <= n <= 100000
