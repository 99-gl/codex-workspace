# 215. 数组中的第 K 个最大元素

## 题目描述

给定整数数组和整数 `k`，求按从大到小排序后的第 `k` 个元素。重复元素会分别计入排名。

## 输入格式

第一行是 `n k`。第二行是 `n` 个整数。

## 输出格式

输出第 `k` 个最大元素。

## 样例

```text
输入：
6 2
3 2 1 5 6 4

输出：
5
```

## 数据范围

- 1 <= k <= n <= 100000
